# گزارش کنترل کیفیت Homa Ghost 2

نسخه: `2.2.0`  
تاریخ بررسی: `2026-08-13`

## نتیجه نهایی

**قبول — آماده بسته‌بندی برای سرور مرزبان**

## تست خودکار قالب

- ۱۷ تست اجرا شد.
- ۱۷ تست پاس شد.
- ۰ تست ناموفق بود.

پوشش تست‌ها:

- تمام Jinja fieldهای مورد استفاده و نبود syntax حل‌نشده
- وضعیت‌های active، limited، expired، disabled، unlimited و on_hold
- QR داخلی و نبود runtime script CDN
- تشخیص Android، iOS، Windows، macOS و Linux
- ۹ کلاینت و چهار مرحله آموزش برای هر راهنما
- نمایش V2Box در هر دو تب Android و iOS با هدایت دانلود به Google Play و App Store
- Deep Linkهای Hiddify، v2rayNG، Happ، Streisand، V2Box، Shadowrocket و Clash Verge Rev
- نمودارهای ۷، ۱۴ و ۳۰ روز، جمع نودها و fallback محلی
- دانلود، آپلود، پینگ و جیتر در مسیر تست کنترل‌شده
- بروزرسانی `/info`، Clipboard، QR کانفیگ و ماندگاری تم
- اعتبار ساختار HTML
- نبود تخلف دسترس‌پذیری با شدت Critical در Axe
- اعتبار نحو Bash نصب‌کننده
- اعتبار نحو و رفتار اصلی نصب‌کننده تک‌خطی، نسخه ثابت، مخزن رسمی، SHA-256 و تحویل به نصب‌کننده محلی
- HTTP smoke test برای صفحه، `/usage`، `/info`، دانلود و آپلود تستی

## تست عملی نصب تک‌خطی

چرخه کامل نصب آنلاین در یک مسیر موقت شبیه‌سازی شد: دانلود ZIP و فایل checksum، تأیید SHA-256، استخراج بسته، اجرای `install.sh`، ساخت بکاپ، نصب `index.html` و QR محلی، و ثبت دو متغیر لازم در فایل `.env`. تمام بررسی‌ها پاس شدند و هیچ مسیری از نصب واقعی مرزبان در محیط تست تغییر نکرد.

## بازبینی مرورگر واقعی

| نمای بررسی‌شده | عرض داخلی | اسکرول افقی | نتیجه |
|---|---:|---:|---|
| دسکتاپ | ۱۳۴۸px | ۰px | قبول |
| تبلت | ۷۵۳px | ۰px | قبول |
| موبایل | ۳۷۵px | ۰px | قبول |

موارد تعاملی بررسی‌شده:

- تغییر پوسته تاریک/روشن و تغییر رنگ محاسبه‌شده صفحه
- فعال‌شدن نوار دسترسی سریع موبایل فقط در نمای موبایل
- انتخاب تب iOS و نمایش راهنمای Streisand
- چهار مرحله راهنما و لینک رسمی App Store
- نمایش داده نمودار، QR، کانفیگ‌های مستقیم و تمام وضعیت‌های اصلی صفحه
- افزایش حداقل اندازه متن پس از بازبینی بصری برای خوانایی نمایشگرهای پرتراکم

هیچ خطای JavaScript مربوط به قالب در Console مشاهده نشد. پیام‌های ثبت‌شده متعلق به افزونه داخلی محیط مرورگر تست بودند و از URL قالب صادر نشده‌اند.

## تست Build

- build تولیدی در پنج مرحله کامل شد.
- Worker خروجی و manifest میزبانی اعتبارسنجی شدند.
- smoke test خروجی رندرشده: ۱ از ۱ پاس.

## نکته پیش از نصب

مقدار `https://t.me/YourSupport` در `APP_CONFIG` عمداً placeholder است، چون آیدی پشتیبانی واقعی ارائه نشده بود. پیش از نصب آن را عوض کن. تا قبل از تنظیم، دکمه پشتیبانی به راهنمای داخلی صفحه می‌رود و لینک اشتباه باز نمی‌کند.

Endpointهای واقعی تست سرعت به دسترسی HTTPS و CORS سرویس Cloudflare وابسته‌اند. منطق موفقیت، Timeout و شکست با endpoint کنترل‌شده تست شده است؛ نتیجه عددی روی سرور نهایی به شبکه کاربر وابسته خواهد بود.
