# گزارش کنترل کیفیت Homa Ghost Subscription

نسخه: `2.3.0`  
تاریخ بررسی: `2026-08-13`

## نتیجه

**قبول — ۲۲ تست از ۲۲ تست پاس شد. مجموعه کامل پس از رفع ایراد rollback سه بار متوالی با نتیجه یکسان اجرا شد.**

## پوشش خودکار رابط

- رندر همه وضعیت‌ها: active، limited، expired، disabled، unlimited و on_hold
- نبود Jinja حل‌نشده و استفاده از فیلدهای رسمی مرزبان
- QR داخلی، کپی لینک/کانفیگ و نبود runtime script از CDN
- تشخیص پنج سیستم‌عامل و پوشش ۹ کلاینت
- V2Box در Android و iOS/iPadOS با فروشگاه رسمی هر پلتفرم
- Deep Linkهای پشتیبانی‌شده و مسیر `clash-meta`
- نمودارهای ۷/۱۴/۳۰ روزه، fallback محلی و پیش‌بینی مصرف
- تمدید هوشمند برای حالت‌های بحرانی و نزدیک به پایان
- گزارش امن و بررسی نبود URL، token یا config در آن
- بروزرسانی `/info`، تست دانلود/آپلود/پینگ/جیتر، Clipboard و تم ماندگار
- اعتبار HTML و نبود تخلف دسترس‌پذیری با شدت Critical در Axe
- HTTP smoke test صفحه، `/usage`، `/info` و endpointهای تست شبکه
- نصب موفق artifact معتبر و رد checksum دست‌کاری‌شده، نام checksum اشتباه، ZIP traversal، symlink و ZIP بیش‌ازحد بزرگ

## پوشش چرخه نصب

- اعتبار نحوی `install.sh`، `install-online.sh`، `homa-sub` و تست یکپارچه
- نصب در محیط موقت بدون دست‌زدن به مسیر واقعی مرزبان
- ایجاد `index.html`، QR محلی، `config.js` و فرمان اجرایی `homa-sub`
- مجوز `0600` برای تنظیمات پایدار
- شخصی‌سازی فارسی و escape صحیح quote در JavaScript
- رد رنگ نامعتبر و نویسه کنترلی بدون تغییر تنظیمات
- نصب دوباره و حفظ hash تنظیمات
- بکاپ، restore و رد manifest خراب یا فایل دست‌کاری‌شده با checksum پیش از تغییر مقصد
- rollback خودکار هنگام شکست ساختگی `marzban restart`
- نگه‌داری حداکثر سه بکاپ
- مهاجرت برند و لینک‌های نسخه ۲.۲ به تنظیمات پایدار
- حذف امن قالب و پاک‌شدن متغیر فعال‌سازی از `.env`

## بازبینی منابع رسمی

در تاریخ بررسی، مستندات و سورس رسمی Marzban وجود `CUSTOM_TEMPLATES_DIRECTORY`، `SUBSCRIPTION_PAGE_TEMPLATE`، `/{token}/info`، `/{token}/usage` و `/{token}/clash-meta` را تأیید کردند. صفحه‌های رسمی Google Play و App Store نیز شناسه‌های V2Box استفاده‌شده در قالب را تأیید کردند.

## بررسی بسته انتشار

- ZIP با مسیر ریشه `Homa-Ghost-Subscription-v2.3.0/` ساخته و با `unzip -t` بررسی شد.
- checksum تولیدی در `dist/Homa-Ghost-Subscription-v2.3.0.zip.sha256` نگه‌داری می‌شود.
- نصب‌کننده تک‌خطی از روی یک HTTP server محلی و مسیرهای کاملاً موقت اجرا شد.
- دانلود، اعتبارسنجی SHA-256، کنترل ساختار ZIP، استخراج، نصب، ساخت بکاپ و `homa-sub doctor` همگی پاس شدند.

## محدودیت محیط تست

رفتار DOM، ساختار responsive، تعاملات و دسترس‌پذیری به‌صورت خودکار بررسی شده‌اند. در این محیط مرورگر گرافیکی واقعی در دسترس نبود؛ بنابراین برای نسخه ۲.۳ ادعای بازبینی تصویری دستی جدید ثبت نشده است. تست سرعت واقعی نیز به شبکه و CORS سرویس مقصد کاربر وابسته است؛ مسیر موفقیت، خطا و timeout با endpoint کنترل‌شده آزمایش شده است.
